part of item_containers_widget;

class _ItemContainersDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('item_containers_desktop'),
    );
  }
}